---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<div class={`bg-white rounded-2xl shadow-sm hover:shadow-md border border-ink-100 p-6 transition-shadow ${className}`}>
  <slot />
</div>
